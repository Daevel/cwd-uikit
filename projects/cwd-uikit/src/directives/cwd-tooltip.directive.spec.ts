import { CwdTooltipDirective } from './cwd-tooltip.directive';

describe('CwdTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CwdTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
